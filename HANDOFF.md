# HANDOFF — 이지 하네스

## 🔧 완료 (2026-07-23, v0.1.4로 배포됨) — 실사용 테스트로 찾은 출시 차단급 버그 3건

**제작자 본인이 v0.1.3 실제 dmg로 처음 실사용해보다 발견.** 커밋 a471745, v0.1.4에 포함.

### BUG-2 (심각) · 맥에서 자식 프로세스에 PATH를 안 넣어줬다
Finder로 켠 GUI 앱은 PATH가 `/usr/bin:/bin:/usr/sbin:/sbin` 뿐이다. `process.rs`·`pty.rs`의 PATH 주입이 `#[cfg(windows)]`로 윈도우 전용이었다.
→ **OpenClaw 설치 실패**: 설치 스크립트가 node를 못 찾아 Homebrew를 sudo로 깔려다 TTY 없어 중단.
→ 실측: GUI PATH에서 `node`·`bun`·`npx`·`claude` **전부 못 찾음**(git만 찾힘).

**수정**: 로그인 셸 PATH를 한 번 구해 캐시하고 모든 자식에 주입.
- `$SHELL` 사용(`is_file()` 검증, 기본 `/bin/zsh`)
- **`-ilc`(대화형) 먼저 → `-lc` 폴백.** `.zshrc`에서 PATH 늘리는 사람이 많다(실측: `-lc` 17개 vs `-ilc` 30개, bun이 `-lc`에선 누락)
- 스레드+타임아웃(5s/3s), 실패하면 조용히 기존 동작으로
- 출력에 마커를 붙여 프로필 배너와 분리, **조각 단위 검증**(fish의 공백 조인 출력 거부 → 폴백)
- 상속 PATH를 뒤에 병합(기존에 되던 건 계속 됨), 상대 경로 조각 제거
- `lib.rs setup()`에서 백그라운드 프리워밍(첫 화면이 셸을 기다리지 않게)

### BUG-1 (중간) · 첫 화면이 이미 있는 Node.js를 "준비물 1개 필요"로 표시
같은 뿌리. BUG-2 수정으로 자동 해결(`probe.rs`가 같은 러너를 탄다).

### BUG-3 (중간) · 실패한 단계가 진행 표시줄엔 완료(✓)로 보였다
실패 뒤 도착하는 `done` 이벤트가 `Math.max`로 phase를 밀어올렸다. 아무것도 안 깔렸는데 "마무리만 실패"로 오해하게 됨.
→ `runReducer`에서 error가 있으면 phase 동결 + `WizardStepper`가 멈춘 단계를 `!`(빨강)로 표시. 재시도는 `attempt` 변경 시 상태를 통째로 초기화하므로 영향 없음.

### 검증
- CI 5종 전부 통과 (fmt·clippy -D warnings·cargo test 106개·vitest 85개·build)
- **회귀 방지선 확인**: 주입 블록을 지우면 `runner_gives_children_the_resolved_path` 가 실제로 FAILED
- GUI 환경 실측: node·bun·npx·claude 전부 ✗ → **전부 ✓**
- 4개 에이전트 적대적 검토 평균 8.1/10, high 0건. 지적된 medium 5건 중 4건 반영

### 남은 한계 (인지하고 넘어감)
- `pty.rs` 주입에는 회귀 테스트가 없다(PTY 테스트 구성 비용이 큼)
- 타임아웃 후에도 셸 스레드가 `output()`에 매달려 남을 수 있다. 앱 수명당 최대 2개라 방치. stderr는 버려서 흔한 멈춤 경로는 막음
- **실제 앱 빌드로 OpenClaw 재설치 end-to-end 재현은 아직 안 함.** 메커니즘은 증명됨

### ✅ end-to-end 검증 완료 (v0.1.4 실제 빌드)
새 dmg를 받아 설치하고 **OpenClaw 설치를 다시 돌려 성공**했다. `/Users/ybg/.npm-global/bin/openclaw` 2026.7.1-2 실제 설치됨. node는 v22.23.1 그대로(Homebrew 강탈 없음). PTY 안에서 OpenClaw이 `Claude Code (installed)` 를 감지 → `pty.rs` 주입도 작동 확인.

---

## 🔧 완료 (2026-07-23, v0.1.4로 배포됨) — 인증 레시피 낡음 (claude-code · opencode)

**실사용 중 발견.** OpenClaw 인증 화면의 안내가 "질문이 나오면 Enter로 기본값"인데 실제로는 자유 대화형이라 **영어로 `yes`를 타이핑**해야 했다. 도구가 온보딩을 바꾸는 동안 레시피 문구만 낡은 것. 같은 문제가 다른 두 개에도 있었다.

### 고친 것 (레시피 JSON만. 스키마·UI 변경 0, 호환성 위험 0)

| 도구 | 전 | 후 |
|---|---|---|
| **claude-code** | `interactive_terminal` + 맨 `claude`(풀 채팅 TUI). 안내는 "로그인 안내 나오면 Enter, 끝나면 `/exit`" | **`browser_login` + `claude auth login`.** 브라우저만 열리고 타이핑 없음. verify에 `claude auth status` 의 `"loggedIn": true` 검사 추가 |
| **opencode** | 맨 `opencode`(풀 채팅 TUI). 안내는 "까만 창에 `/connect` 입력" | **`opencode auth login`** 자격증명 전용 화면. 안내도 현실에 맞게 |

**왜 이게 문제였나**: 둘 다 **이미 로그인된 사용자**에게는 안내가 완전히 헛돌았다. 로그인 안내가 안 나오고 그냥 채팅창이 뜬다(실측: `claude auth status` → `loggedIn: true`). 선례는 `codex.json` 이 이미 `browser_login` + `codex login` + verify `codex login status` 로 정확히 구현하고 있었다.

**주의**: 터미널 자체는 여전히 뜬다(`pty_session` 이라서). 개선된 건 **채팅창 대신 로그인 전용 화면이 뜨고 타이핑할 게 없어진 것**이다. UI는 `auth.pattern` 을 렌더링에 안 쓰고 `guide` 만 쓴다.

### 새 회귀 테스트 2개
- `auth_steps_use_dedicated_login_commands_not_bare_tui` — 맨 도구 이름 실행으로 되돌아가는 걸 막는다
- `auth_guides_do_not_mention_removed_steps` — 없어진 조작(`/exit`·`/connect`)을 안내에 남기는 걸 막는다. **실제로 작성 중 내 카피를 잡았다**("`/exit` 를 입력할 필요 없어요" → 초보자가 몰랐던 개념을 꺼내서 없다고 말하는 문장)

### `BUNDLED_MIN_VERSION` 1 → 2 (중요)
앱 내장 레시피가 바뀌었으므로 올렸다. **안 올리면 사용자 기기에 캐시된 낡은 번들(v1)이 새 내장 레시피를 이겨서 수정이 안 켜진다.** 운용 규칙을 CLAUDE.md에 명문화했다.

### 원격 번들은 아직 안 밀었다 (의도적)
스키마를 안 바꿔서 옛 앱도 읽을 수는 있다. 그런데 새 레시피가 `claude auth login`·`opencode auth login` 을 쓰므로, **옛 CLI 버전에 그 서브커맨드가 없는 사용자는 인증이 깨진다.** 앱 업데이트와 함께 나가는 게 안전하다. 번들 게시는 v0.1.4 확산을 지켜본 뒤 `bundleVersion: 2` 로.

---

## 📋 다음 (v0.1.5 예정) — A+ 동의 화면 + OpenClaw 비대화형

사용자 결정: **덩어리 1(claude-code·opencode)만 v0.1.4에, OpenClaw 동의 인프라는 v0.1.5로.**

### 설계는 나왔다 (에이전트 3개 조사 결과)
**핵심 발견**: `Step` 은 내부 태그드 enum이라 `deny_unknown_fields` 를 못 건다 → **기존 variant 안의 선택 필드만이 옛 앱을 안 깨는 유일한 확장 통로**다(실측 검증됨).

**설계**: `input_secret` 스텝에 `consent` 선택 필드를 달고, 동의를 **명령을 완성시키는 값**으로 쓴다. `{{secret:...}}` 로 `--accept-risk` 를 주입 → 동의를 안 하면 플래그가 안 들어가고, `--non-interactive` 는 `--accept-risk` 없이는 OpenClaw 스스로 거부한다. **UI를 우회해도 실행이 물리적으로 성립하지 않는다(fail closed).** 옛 앱은 암호칸을 보고 아무 값이나 넣어도 실패한다.

파일 약 20개(백엔드 6·프런트 6·테스트·번들 운용). 배포는 **앱 먼저 → 확산 확인 → 번들 v2** 순서.

### ⚠️ 시작 전에 풀어야 할 미해결 문제
**`--non-interactive` 는 `--auth-choice` 를 요구하는데, `skip` 으로 넘기면 모델 미설정이 되어 기존 verify(`openclaw models status`)가 실패한다.** `--auth-choice claude-cli` 고정도 안 된다(Claude Code 미로그인 사용자에게 재시도 없이 fail-fast). **동의 다음에 모델 인증을 어떻게 붙일지가 아직 설계 안 됨.** 여기부터 정해야 한다.

검증 시 주의: 실기기에서 `openclaw onboard --non-interactive` 를 그냥 돌리면 대표님 실계정 설정을 덮어쓸 수 있다. `OPENCLAW_STATE_DIR`/`--profile` 로 격리된 프로필에서 해야 한다.

### 그 밖에 남은 것
- `pty.rs` PATH 주입에는 회귀 테스트가 없다(PTY 테스트 구성 비용)
- UX 관찰 3건: 준비물 정체 안 알려줌 · 첫 화면에 도구 이름 없음 · "3분" 약속 미검증
- OpenClaw auth 진입 게이트 없음 — 이미 로그인된 사용자도 pty를 무조건 띄운다(`step_runner.rs` 변경 필요)

> ⚠️ **노출 실험 판정(7/29)에 영향**: 다운로드 4명 중 누군가 이 버그를 만났을 수 있다. "수요가 없다"가 아니라 "제품이 안 됐다"일 가능성. v0.1.4 배포 후 재실험을 검토할 것.

---

## 🔍 QA 스윕 결과 (2026-07-24 새벽, 에이전트 140개·코드/문서 대조만·실기기 실행 없음)

전체 보고서: `~/.gstack/projects/needsbuilder-easy-harness/qa-sweep-20260724.md`
CI 5종 전부 통과. 발견 62건 → 반박 검증으로 14건 기각 → 48건 생존. **실해급(🔴) 5건, 대표님 결정 대기**:

1. **재오픈 롤백 데이터 손실 (구조)** — 이미 설치된 도구 카드를 다시 눌렀다 실패하면 롤백이 멀쩡한 바이너리를 지우고, installed.json은 "설치됨"으로 남음. v0.1.5 openclaw 사고와 동일 계열. 수정에 앱 릴리스 필요 (`plan.rs`·`step_runner.rs`)
2. **미패치 롤백 레시피 2건** — lazycodex 맥(rollback=완전제거 명령, CONFIRMED)·hermes 윈도우(약함). 원격 레시피 핫픽스로 수정 가능하나, 원격 채널이 현재 죽은 상태(게시본 v1 < min 2)라 번들 v3 게시 + 앱 재시작 필요
3. **PATH 공백 세그먼트 회귀** — VS Code 'code' 명령 설치한 흔한 맥에서 로그인셸 PATH 확장이 통째로 꺼져 v0.1.3의 "설치 실패"가 재현됨. `process.rs:155-160` 술어가 공백 경로를 거부 + OnceLock 영구 캐시. 사실상 한 줄 수정, 앱 릴리스 필요
4. **인텔 맥 다운로드 버튼 없음** — 랜딩이 macArm만 렌더. 인텔 맥 방문자는 받을 길이 없거나 잘못된 dmg로 "손상됨" 오류. **노출 실험 다운로드 지표를 직접 오염**. 웹 배포만으로 수정 (`DownloadButton.tsx`)
5. **시크릿 셸 주입 (korean-law-mcp 한정)** — 인증키 칸에 셸 특수문자 붙여넣으면 임의 명령 실행 가능. 실확률 낮음, 보안이라 동결 예외 후보

🟡 7/29 이후 수정 다수 (k-skill --all이 미설치 도구 폴더 70곳 오염, 안내 문구≠실화면 4건, 가짜 verify 3건, 원격 레시피가 실행 중 앱에 반영 안 됨, 윈도우 winget 단일 의존 등) + ⚪ 기록만 다수. 상세는 보고서.

**스윕의 한계**: 전부 코드·업스트림 문서 정독이지 실행이 아님. ③(진행바) 원본 버그 재현, 롤백 실측, 실제 로그인 화면 대조, 깨끗한 신규 기기, 윈도우 매트릭스는 실기기 필요.

---

## 🔴 지금 상태 (2026-07-23) — 노출 실험 진행 중 (판정 7/29) · 개발 동결

**/office-hours 전략 세션 결과: 새 기능 개발을 멈추고, 홍보 1회 후 7일 뒤 계속할지 접을지 판정한다.**

설계 문서(근거·기각된 접근 5개·판정 기준 전문):
`~/.gstack/projects/needsbuilder-easy-harness/ybg-main-design-20260722-230809.md`

### 왜 이렇게 됐나 (3줄 요약)
- 이지 하네스의 전제인 "설치가 어렵다"가 무너지는 중이다. **초보자가 쓰는 도구(Claude Code·커서·안티그래비티)는 이미 설치가 쉽고, 설치가 어려운 도구(그록빌드·Gemini CLI)는 초보자가 안 쓴다.** Claude Code는 의존성 0 네이티브 인스톨러, ComfyUI도 공식 데스크톱 앱으로 이동(2026-07-22 확인).
- 검증된 수요 신호는 **이지클로 때 AI 강사 2명이 "강의에 써도 되냐"고 물어본 것 하나뿐**이고, 그건 반년 전 오픈클로 유행기 일이다. 유행이 꺼지자 문의도 꺼졌다.
- 단 **이지 하네스 자체는 한 번도 노출된 적이 없다**(공개 2026-07-22 13시, 홍보 0회). 측정 없이 접는 건 결론이 아니라 포기라서, 홍보 1회 후 판정하기로 했다.

### 1라운드 실측 (2026-07-23 새벽, 게시 +11시간) — **유효성 게이트 통과, 실험 유효**

2026-07-22 약 13시에 스레드 게시(레포 공개와 거의 동시). 11시간 뒤 측정.
글: https://www.threads.com/@needsbuilder/post/DbGH33dgQtn

| | 이 글 | 채널 기준 |
|---|---|---|
| 조회 | **1,400** | 최근 게시물 실측 평균 714 · 중앙값 302 · 잘 되는 글 1.9만~19만 |
| 반응 합계 | 60 (좋35·댓14·리포3·공유8) | 30일 6,197 |
| **참여율** | **4.3%** | **0.57%** |
| 실다운로드 | **4회** (dmg 2·exe 2) | |
| 스타 | 외부 5명 | (요청 문구 넣어서 오염 → 판정 제외) |
| 문의·이슈 | **0건** | |

**판정: 유효성 게이트 통과. 1라운드 유효. 실험 진행 중 → 판정일 2026-07-29**

**게시물별 조회 실측(최근 8개)**: 2,200 / **1,400(이지 하네스)** / 1,400 / 1,000 / 422 / 182 / 178 / 175 / 157
→ **평균 714 · 중앙값 302.** 이지 하네스는 **평균의 2배, 중앙값의 4.6배.**

> ⚠️ 초판 분석은 "채널 평균 4,000~6,000, 도달 1/3, 무효"로 판정했다가 **뒤집혔다.** 그 평균은 30일 총조회를 짐작한 게시물 수로 나눈 **추정치**였고, 실제 분포는 상위 5개가 27%를 먹는 극단적 쏠림이라 평균으로 나누면 안 됐다. **게이트 판정에도 실측 의무를 적용한다.**

**반응 O, 채택 X — 둘은 별개다**
- 반응: 도달 채널 평균 2배 · **참여율 4.3% = 채널 평균 0.57%의 7.5배** · 외부 스타 5 · 리포스트 3 · 공유 8
- 채택: 실다운로드 **4회** · 문의·이슈 **0건** · "써봤다·깔았다" 댓글 **0건**

**❗ 병목을 아직 모른다 (퍼널 중간이 안 보임)**
조회 1,400 → **랜딩 방문 ?** → 다운로드 4
- 랜딩 30명 중 4명 = 13% → 제품 정상, **병목은 링크 클릭(글)**
- 랜딩 300명 중 4명 = 1.3% → **병목은 제품·랜딩**
- 처방이 정반대다. 이 숫자 없이 결론 금지

**측정 공백 2건**
1. **랜딩(web/)에 애널리틱스 없음** → 조회→클릭 구간 영구 미측정
2. **GitHub 트래픽 API 약 1일 지연** → 7/23 조회 시 7/21까지만 나옴. **7/24 이후 재조회하면 7/22 referrer 복원 가능**

**댓글 정성** — 외부인 4명 전부 농담·리액션. "써봤다·깔았다·어떻게 쓰냐·○○도 넣어줘" **0건.**
가장 중요: 후속 댓글로 **"설치하다 막혀서 포기한 도구 있어?"라고 직접 물었는데 아무도 도구 이름을 안 댔다.** 판정일까지 그대로면 **강한 음의 신호**다.

### 🔄 2차 실측 (2026-07-23 밤, 게시 +약 1.5일)

| 지표 | 11시간 차 | 1.5일 차 | 비고 |
|---|---|---|---|
| 조회 | 1,400 | **3,000** | 채널 실측 평균 714의 4.2배. A축 ⓔ 기준선(2,142) 초과 |
| 반응 합계 | 60 | **140** (좋72·댓14·리포20·공유34) | 참여율 4.7%. 리포 20이면 ⓖ(외부 재게시 3건+)도 충족권 |
| 댓글 | 14 | **14 (증가 0)** | 확산은 느는데 대화는 안 늘어남. 도구명 무응답도 그대로 |
| 설치파일 다운로드 | 4회 | 누적 **15회** (v0.1.2~0.1.5 dmg+exe 합계. 자체 검증분 제외 시 외부 추정 10~12회) | ⓑ 기준(증분 20회) 미달 |
| 스타 | 외부 5 | 외부 **7** (kindsusu·Iris-jo 추가) | 참고 지표 (판정 제외) |
| 문의·이슈 | 0 | **0** | 레포 이슈 4건은 전부 본인 PR |

**퍼널 중간이 처음 보였다** (GitHub 트래픽 API, 7/22분 복원 완료):
- 레포 방문 19회 / **순방문자 7명**. 유입: 랜딩 8회(4명) · github 4회(2명) · 스레드 직접 2회(2명)
- 랜딩→레포가 4명뿐. 단 다운로드는 릴리스 자산 직링크로도 가능해 레포 방문이 다운로드의 상한은 아니다
- v0.1.3 latest.json 8회 수신 = **설치된 앱이 켜져서 업데이트 확인을 한 흔적**(본인 기기 포함). 깔고 실행까지 간 사람이 있다는 약한 신호
- 7/23분은 7/24 이후 재조회로 복원

**현재 추세 요약**: A축(인지도)은 충족 라인 위, B축(제품 수요)은 아래. 이대로 7/29가 되면 판정표 **②칸(제품은 접고 콘텐츠는 계속 = 목표 관점 성공)** 으로 수렴. 단 판정은 7/29 수치로만 한다.

### ⚠️ 실험 규율 이탈 기록 (판정 해석 시 반영)

1. **동결 중 릴리스 2건**: v0.1.4(맥 PATH 미주입으로 설치 실패 + 인증 안내 낡음), v0.1.5(롤백이 사용자 OpenClaw 설정 삭제 + 온보딩 선택형). 전부 결함 수정이고 신기능 0. 동결 예외 규정(보안 취약점)에 형식상 안 맞지만, 설치 실패·설정 삭제는 다운로드 사용자에게 실해를 주는 결함이라 방치하면 실험이 "고장난 제품의 수요"를 재게 된다. **판정 시 유의**: 7/22~23 초기 다운로더 일부는 설치 실패를 겪었을 수 있다 → B축 미충족이 나오면 이 오염 가능성을 함께 기록.
2. **사전 등록이 게시(7/22 13시)보다 늦었다**: 기준 잠금 커밋이 게시 후다(이 커밋). 보완책으로 **수치 조정 없이 원안 그대로** 확정한다. 결과를 이미 본 상태의 조정은 방향 불문 전부 오염으로 간주.
3. (기존 기록) 1라운드 글에 스타 요청 문구 포함 → 스타는 이미 판정 제외라 영향 없음. 2라운드 글에는 금지.

### ✅ 7/29 판정까지 남은 일

- [ ] **강사 발송** (미실시. 안 하면 B축 ⓐ 영구 미측정. **답신 시간을 고려하면 7/24~25 중 발송 필수**)
      - 링크에 `?utm_source=instructor` → 공개 홍보 경로와 분리
      - 아는 강사 2명: 기록만, **판정 제외**
      - **모르는 강사·교육기관 5곳 이상**: ⓐ는 여기서만 카운트. 명단 초안 아래 ↓
- [x] ~~7/22 유입 복원~~ **완료 (2026-07-23)**: 위 2차 실측 참고. 7/23분은 **7/24 이후** 재조회
- [ ] **랜딩 애널리틱스 추가 여부 결정** (동결 예외 제안: 앱이 아니라 web/ 계측이고, 없으면 앞으로도 영구히 못 잼)
- [ ] **7/29 판정** — 유효성 게이트는 이미 통과. 2×2 표 적용

**2라운드는 판정 결과가 ③(한 번 더)일 때만.** 그때 바꿀 것: 링크를 첫 댓글로 · 서사 형식 · 별 요청 금지

**1일차 — 준비**
- [x] ~~채널 실측~~ **완료 (2026-07-23)**: 팔로워 4,069 · 30일 조회 108.4만 · 30일 반응 6,197
      → **최근 게시물 실측 평균 714 · 중앙값 302** (이지 하네스 글 제외 8개 기준)
      → **유효성 게이트 = 도달 714 이상** (11시간 차 1,400으로 이미 통과)
      → **A축 ⓔ = 평균 3배 = 조회 2,142 이상**
      → ~~"평균 4,000~6,000 · 3배 = 1.2만~1.8만"~~ 은 30일 총조회 기반 **추정치라 폐기** (설계 문서 v3 정정 반영: 상위 5개 19만/5.3만/2.1만/2.0만/1.9만이 전체 27%를 먹는 쏠림 분포라 총조회 나누기 게시물 수는 무의미)
- [x] ~~기준선 스냅샷~~ **완료 (2026-07-22 게시 직전 기준)**: 스타 0(외부) · dmg 1회(자체 검증) · 이슈 0
      → 1라운드 후 실측: 스타 외부 5 · 실다운로드 4 · 이슈 0
- [x] ~~판정 기준 확정~~ **확정 (2026-07-23): 원안 수치 그대로, 조정 없음** — ⓑ 다운로드 증분 20회 · ⓓ 실제 문의 5건 · ⓐ 강사 행동증거 1건(모르는 풀에서만). 게시 후 잠금이라 결과를 본 상태의 조정은 방향 불문 오염으로 간주해 유도식을 쓰지 않는다
- [x] ~~HANDOFF 기록 + `git commit` + `git push`~~ **완료 (2026-07-23, 이 커밋이 사전 등록 잠금. 이후 기준 수정 금지)**
- [x] ~~모르는 강사·교육기관 5곳 명단~~ **초안 완료 (아래 ↓). 발송 전 대표님 확인 2가지: ① 아는 강사 2명과 겹치는 항목 제외 ② 발송 문안 승인**
- [ ] **아는 강사 2명 연락처** 아직 있는지 확인 (없으면 소속 채널에서 재탐색, 시간 더 듦)

**강사·교육기관 후보 명단 초안 (2026-07-23 웹 리서치, ⓐ 판정 풀 후보 10곳)**

인프런 강사 (연락은 프로필의 유튜브 비즈메일·블로그 등 외부 채널 우선, 없으면 강의 페이지 문의):
1. **이정환(Winterlood)** — "한 입 크기로 잘라먹는 바이브코딩 (with Claude Code)". 입문자 톤이라 적합도 최상
2. **짐코딩** — "클로드 코드 완벽 마스터". 입문용 유튜브 채널 보유, 수강생 규모 큼
3. **널널한 개발자** — "30년차 개발자의 실전 바이브 코딩". 유튜브 채널 보유
4. **딩코딩코** — "The 10x AI-Native Developer"

패스트캠퍼스·교육기관 (홈페이지 문의 폼·대표 메일):
5. **전현준** — 패캠 "하네스 엔지니어링: Claude Code·Codex 완벽 가이드" (패캠 인기 TOP3)
6. **정진호** — 패캠 x 정진호 바이브 코딩, jvibeschool.com 운영
7. **패스트캠퍼스 KDC "코딩 몰라도 누구나! AI 바이브코딩"** 운영팀 — 국비(내일배움카드) 비개발자 과정. **7/22~8/26 진행 중이라 수강생이 지금 설치 구간을 지나는 중**
8. **모두의연구소** — aiffel.io/vibecoding 재직자 바이브코딩 부트캠프
9. **AI 캠퍼스(ai-campus.kr)** — 클로드 기업교육 전문 기관
10. **팀스파르타(스파르타클럽)** — 비개발자 코딩 교육 최대 규모

**2일차 — 게시 (약 3시간)**
- [ ] **제품 소개 글 작성** (실패담 아님)
      - 넣을 것: 데모 영상·화면(`web/public`에 있음), 지원 도구 목록, 다운로드 링크, "내 도구 추가하기"
      - ❌ "아직 부족하지만" 같은 겸손한 변명 · ❌ "스타 눌러주세요"(지표 오염)
- [ ] **홍보 1회 게시** (같은 날 여러 채널 동시 게시 = 1라운드)
- [ ] **바로 미리알림 등록**: "이지 하네스 노출 실험 판정" → 게시일 +7일 오전 9:30
- [ ] **강사 발송** (링크 끝에 `?utm_source=instructor` 붙이기)
      - 아는 2명: "그때 물어보셨던 것의 후속" 프레임 → 답신은 기록만, **판정엔 안 넣음**
      - 모르는 5곳 이상: 같은 메시지 → **여기서 나온 것만 ⓐ로 셈**

**3~4일차**
- [ ] **게시 24~48시간 내 조회수·노출수 캡처** (나중에 지표가 사라질 수 있음)

**7일 후 — 판정 (30분)**
- [ ] 유효성 게이트 통과했나? (도달 ≥ 평균) → 미달이면 **무효, 판정 안 함**
- [ ] A축·B축 각각 충족 여부 확인 → 2×2 표에서 칸 찾기
- [ ] 강사 답신 원문을 설계 문서에 그대로 붙여넣기 (사후 검증용)
- [ ] 결과와 다음 행동을 HANDOFF에 기록

**이 7일 동안 절대 안 하는 것**: 앱 기능 추가 · 릴리스 · 재게시 · 기준 수정
(예외: 실제 악용 가능한 심각한 보안 취약점 → 즉시 패치, 판정에 영향 없음)

### 사전 등록된 판정 기준 (측정: 홍보 +7일 = 2026-07-29)

**✅ 확정 (2026-07-23): 원안 수치 그대로. 이 커밋의 푸시가 잠금이며 이후 수정 금지.**

참고 실측(2026-07-22): 스타 3개(외부 2명: rhodewhalin·kayboram), aarch64 dmg 각 1회(자체 검증 추정), 문의 0건. **실제 기준선은 게시 직전에 다시 스냅샷을 떠서 쓰고, 모든 지표는 증분으로 센다.**

**0단계 유효성 게이트**: 도달이 자기 채널 최근 10개 평균(실측 714) 미만이면 **이번 라운드 무효**. 판정 안 하고 다른 채널로 다시. (없으면 "홍보가 약해서 실패"와 "수요가 없어서 실패"가 구분 안 됨) → **1라운드는 통과함**

**A축 인지도** (셋 중 1개): 도달·조회 평균 3배+ / 저장·공유 평균 3배+ / 외부 언급·재게시 3건+
**B축 제품수요** (셋 중 1개): 강사 행동증거 1건+ / dmg 증분 20회+ / 실제 문의 5건+
- **강사 행동증거 1건**의 정의: ①구체 일정 지목하며 쓰겠다 ②배포용 링크·자료 요청 ③실제 공지·자료에 링크 삽입 — **중 2개 이상.** "좋네요·나중에 써볼게요"는 0건. **모르는 강사 풀에서만 카운트**
- 스타는 판정에서 제외(조작 쉬움). 참고 지표로만 기록

| | B축 충족 | B축 미충족 |
|---|---|---|
| **A축 충족** | ① **계속 간다** (F 철회, Approach B 재검토) | ② **제품은 접고 콘텐츠는 계속** (목표 관점 성공) |
| **A축 미충족** | ③ **한 번 더** (채널 문제, 2라운드) | ④ **접는다** (유지보수 + 실패담 글) |

**기준 조정은 4개 규칙 전부 지킬 때만**: ①최근 10개 실측 선행(없으면 조정 불가) ②유도식 고정(`ⓑ = 기대도달 R × 0.5%`, R만 조정 가능) ③커밋·푸시로 잠금 ④원안 대비 50% 미만 불가

### 실험 중 규율 (중요)
- **7일간 앱 개발·릴리스 0.** 단 **실제 악용 가능한 심각한 보안 취약점은 예외**(즉시 패치, 판정에 영향 없음)
- **라운드당 홍보 1회, 전체 최대 2라운드.** 2라운드 판정표에는 ③칸이 없다
- **결과를 본 뒤 기준을 바꾸지 않는다** (푸시된 커밋이 증거)
- **홍보 글에 "스타 눌러주세요" 금지**

### 기각된 방향 (다시 논의하지 말 것, 근거는 설계 문서)
- 글로벌 오픈소스 카탈로그: Pinokio(스타 7.8천)·UniGetUI(스타 2만 1600)가 점유. 진입 수단 없음
- 유행 도구 레시피 선점(그록빌드 등): 초보자가 안 쓰고 설정도 필요 없음
- 강의별 맞춤 인스톨러: 설치·설정법 자체가 강의 커리큘럼이라 강의 사업과 충돌 + 공급이 전량 본인 노동

---

## 완료 (2026-07-22) — Apache-2.0 오픈소스 공개 ✅

**https://github.com/needsbuilder/easy-harness 가 public 이다.** push + `gh repo edit --visibility public` 완료, 익명 접근·라이선스 인식(Apache-2.0)·README 이미지 렌더링까지 검증함. 토픽 12종·홈페이지(easyharness.needslab.ai)·설명 설정 완료.

### 공개 직후 추가 완료 (커밋 c8f6d3c·968a9e7)
- **이슈 템플릿 2종**(`.github/ISSUE_TEMPLATE/`): `new-tool.yml`(도구 정보·설치 명령·로그인 방식을 구조화해 받아 되묻는 왕복 제거) · `bug.yml`(비개발자용 문구). `config.yml`은 보안 신고를 Security Advisory로 유도.
- **PR 템플릿**: 확인한 OS·테스트 추가 여부·5개 검사 체크박스.
- **랜딩 스타 유도**: `GITHUB_REPO`를 릴리스 레포 → **소스 레포**로 교체(스타는 소스에 쌓여야 함). Nav에 스타 개수, `OpenSource.tsx` 섹션 신규(별 주기 + 내 도구 추가하기). `useGithubStars`는 **0개면 숨긴다**(별 0은 사회적 증거 부재를 광고하는 역효과). API 미인증 60회/시간 제한에 걸려도 버튼은 그대로 보이게 실패를 삼킨다.
- 검증: 실브라우저로 데스크톱·모바일 렌더링, 가로스크롤 없음, 콘솔 에러 없음 확인.

### 소셜 프리뷰 완료
`web/public/og.png`를 레포 Settings에 업로드함. **gh CLI에는 이 API가 없어서** playwriter로 사용자 크롬에 붙어 `#repo-image-file-input`에 `setInputFiles`로 넣었다. 검증은 레포 HTML의 `og:image`가 `repository-images.githubusercontent.com/...`인지로 한다(기본값이면 `opengraph.githubassets.com`).
- 함정: 업로드 성공해도 "Upload an image to customize..." 안내 문구는 그대로 남는다. 콘솔의 503 로그도 무관한 리소스 것이라 실패로 오판하기 쉽다. **판정은 반드시 og:image로.**

### 이슈 템플릿 실측 결과
- YAML 폼은 **GraphQL `issueTemplates`에 안 잡힌다**(빈 배열). 마크다운 템플릿만 반환하므로 API로는 검증 불가 → 로그인 상태의 브라우저로 `issues/new/choose`를 봐야 한다.
- **`mailto:` contact_link는 GitHub이 렌더링하지 않는다**(실측). 메일 안내는 SECURITY.md에만 둔다.
- 보안 신고 항목은 GitHub이 "Report a security vulnerability"를 자동 제공하므로 config.yml에 또 적으면 중복이다 → 제거함.

### 아직 남은 것
- Code of Conduct 없음(CONTRIBUTING 말미 "서로에 대한 예의"로 대신 중, GitHub은 인식 못 함) · Discussions 비활성(관리 부담)

### 결정 (2026-07-22)
- **직접 수익화 안 함**. 무료·오픈소스로 인지도를 쌓아 **책·강의·해커톤**으로 수익화하는 방향(사용자 판단). 유료 기능 계획 없음 → 라이선스 분리 구조 불필요.
- **라이선스 Apache-2.0**. MIT 대신 고른 이유는 상표 조항(6조)으로 "이지 하네스" 이름 무단 재배포를 막을 근거가 생기기 때문.
- **소유는 니즈랩, 얼굴은 권용범**. NOTICE=저작권 니즈랩, README "만든 사람"=개인 서사(책·강의는 법인이 아니라 사람이 팖).
- **커밋 이메일은 `ybgwon96@gmail.com` 유지**(전역 설정 그대로). hello@needslab.ai로 바꿨다가 사용자가 되돌림. 레포 로컬 git 설정 없음.
- **히스토리 재작성 안 함**: 태그 v0.1.0~v0.1.2가 있어 SHA가 바뀌면 릴리스↔소스 대응이 깨진다. 과거 커밋 168개의 개인 지메일 노출은 감수.
- **HANDOFF·docs·CLAUDE.md 공개 그대로 둠**(사용자 결정). 홍보용 `스레드용_*.png`는 gitignore.

### 공개 전 안전 점검 완료
히스토리 4만 줄 diff 전수 스캔 → API 키·개인키·.env 커밋 이력 **없음**. 걸린 건 전부 `scan_secrets.rs`의 가짜 테스트 값. Apple Team ID·시크릿 "이름"은 노출되나 값은 아니며 서명된 앱에 이미 들어있어 비밀 아님.

### 만든 문서 (커밋 `8a7b51f`)
`LICENSE`(apache.org 원문) · `NOTICE`(상표 조항) · `README.md`(전면 개편) · `CONTRIBUTING.md` · `SECURITY.md` · package.json·Cargo.toml `license` 필드.

- **CONTRIBUTING이 핵심**: 도구 제작자가 직접 레시피를 PR하게 만드는 게 "사용자 노동 없는 성장 엔진". "JSON 한 개면 되고 Rust·React 몰라도 된다"를 앞세우고, Step 7종·치환자·인증 4패턴·문구 규칙·`real_recipes.rs` 테스트 작성법까지 적음.
- **레시피 PR = 보안 리뷰**로 취급한다고 명시(사용자 PC에서 실제 명령 실행). 서명은 관리자만 하므로 머지≠배포.

### 개인 계정으로 이전 (2026-07-22, 공개 1시간 뒤)
`needslab-ai/easy-harness` → **`needsbuilder/easy-harness`**. 사용자 판단: 목표가 개인 인지도(책·강의)인데 조직 소유면 URL·프로필 노출이 안 잡힌다.
- **조직 레포여도 잔디는 이미 심기고 있었다**(개인 기여 175회 집계 확인). 차이는 ① URL에 계정명 ② 프로필 Repositories 탭 ③ 스타 받은 레포의 주인 ④ 검색 노출.
- 이전 비용 0이었던 이유: 스타 0·다운로드 0·홍보 전이었고, **GitHub이 시크릿 9종을 그대로 이전**하며(공식 문서 확인) 옛 URL도 자동 리다이렉트한다.
- **주의: `needslab-ai`에 다시 `easy-harness` 이름으로 레포를 만들면 리다이렉트가 영구 삭제된다.** 만들지 말 것.
- 저작권은 그대로 니즈랩(NOTICE·LICENSE가 정한다. 레포 위치와 무관).
- `-releases`·`-recipes`는 아직 조직 소유 → 아래 "레포 통합"에서 함께 정리.

### 레포 통합 완료 (2026-07-22, 커밋 9ccb9d9)
**레포를 3개로 나눈 유일한 이유는 "소스가 private"이었다**(M5 설계 문서 확인). public이 되면서 명분이 사라져 하나로 합쳤다. 다운로드 실적이 0회일 때만 가능한 작업이었다(업데이트 주소가 배포된 앱에 박히므로).
- 레시피 번들 → `recipes-bundle/`. `REMOTE_BASE`가 이 레포 raw를 본다. `build_recipes_bundle.sh`가 게시본까지 복사하므로 **커밋·푸시 = 배포**.
- `release.yml`에서 owner/repo를 비워 이 레포에 릴리스 → **`RELEASES_PAT` 시크릿이 더는 안 쓰인다**(레포에는 남아있음, 정리 가능).
- **v0.1.2 자산 11개를 새 레포로 복사했다.** `latest.json`의 링크는 자산 ID 기반이라 옮기면 깨지므로 **파일명 기반 URL로 재작성**했다(다음에 또 옮겨도 안 깨진다).
- 옛 레포 2개는 아카이브. **삭제 금지** — v0.1.2 이하 앱이 아직 그 주소를 본다(아카이브 후에도 raw·download 모두 200 확인).
- 검증: 새 raw 번들 200(82606B, 원본과 동일) · 새 dmg 다운로드 200(11387635B) · 랜딩 다운로드 버튼·푸터 버전(v0.1.2) 정상.

### 공개 후 남은 것
- **Apple Developer가 Individual(개인) 명의** → 서명이 `YONG BEOM GWON`으로 나감. 지금 문제는 없으나 법인·팀 계정 전환 시 재서명 필요(사용자 인지함).
- 인지도→수익 전환 장치 3종이 아직 **없음**(사용자에게 지적한 상태): ① 설치 성공 화면에서 다음 단계로 보내는 통로 ② 이메일 명단(개인정보처리방침에 수집 항목 추가 필요) ③ 제품에 드러난 개인 브랜드.

---

## 완료 (2026-07-22) — 랜딩 정식 주소 확정
`easyharness.needslab.ai`를 정식(canonical) 주소로 확정하고 배포·검증까지 끝. 커밋 `2a8ff9f`.

- **easyharness.com은 구매 불가**: 2018년 등록(호주), 만료 2026-08-14, 빈 페이지 상태. 8년째 미사용이라 **9월 말~10월에 드롭 여부 재확인 가치 있음**. 대안 미등록: easy-harness.com(~$20/년)·easyharness.co·.io·.kr.
- **vercel.json**: `.vercel.app` → needslab.ai 308 영구 이동. **루트(`/`)는 `:path*`가 빈 경로를 안 잡아 규칙을 따로 둬야 한다**(첫 배포에서 홈만 리다이렉트 누락돼 재배포함). `type: "host"`는 openapi.vercel.sh/vercel.json 스키마로 검증한 정식 문법.
- **og.png가 아예 없어서 카톡·슬랙 공유 썸네일이 안 뜨고 있었다**. SPA rewrite 탓에 없는 파일도 200(text/html)으로 나와 그동안 안 드러남. demo-poster.png를 1200x630으로 크롭해 생성. 같은 이유로 robots.txt·sitemap.xml도 없었음 → 추가.
- 검증: `.vercel.app`의 `/`·`/terms`·`/privacy`·`/og.png` 전부 308 → 정식 주소 200. og.png가 `image/png`로 정상 서빙.
- 배포는 여전히 git 연동 아님 → **`vercel deploy --prod --cwd web`**.

---

## 완료 (2026-07-22) — v0.1.3 릴리스 + 업데이트 경로 정비
**v0.1.3 발행 완료**(needsbuilder/easy-harness). 내용은 앱이 보는 주소를 합쳐진 레포로 옮긴 것 하나뿐. 빌드 3종·시크릿 스캔 전부 통과.

### v0.1.2 이하 사용자를 위한 브리지 (중요)
v0.1.2 앱은 **옛 endpoint를 보도록 빌드돼 있어** 새 레포에 v0.1.3을 올려도 업데이트 알림을 못 받는다. 그래서 **옛 `easy-harness-releases` 레포에 v0.1.3 릴리스를 만들고 `latest.json`만 올렸다**(자산 URL은 새 레포를 가리킴). 이러면 옛 앱도 업데이트를 받고, 업데이트 후부터는 새 endpoint를 본다.
- 절차: `gh api -X PATCH repos/... -F archived=false` → 릴리스 생성 → 재아카이브.
- **함정 2개**: ① `gh release create file#이름` 문법이 안 먹혀 자산이 `bridge-latest.json`으로 올라갔다. 앱은 정확히 `latest.json`을 찾으므로 **파일명 자체를 latest.json으로 만들어 업로드**해야 한다. ② 업로드 직후 20초쯤 404가 난다(CDN 반영). 바로 검증하면 실패로 오판한다.
- 검증 완료: 옛 주소·새 주소 latest.json 모두 200, 새 dmg 다운로드 200(아카이브 상태에서도).

### 다음 릴리스(v0.1.4)에 들어갈 것 — 이미 main에 커밋됨(8c06887)
**설정 화면에 "앱 버전" 섹션**(현재 버전 표시 + "새 버전 확인하기" 버튼). 자동 확인이 앱 시작 시 1회뿐이라 그때 네트워크가 끊기면 재시작 전까지 방법이 없던 막다른 길을 없앤다. `useAppUpdate`에 `checkNow()` 추가(배너용 phase는 업데이트가 있을 때만 건드려 기존 동작 불변).
- 테스트 함정: settings 테스트는 **`AppUpdateProvider`로 감싸야** 한다(안 그러면 fallback의 `checkNow`가 항상 uptodate). 이 프로젝트는 vitest globals를 안 써서 **testing-library 자동 cleanup이 안 돈다** → 한 파일에 테스트 여러 개면 직접 `cleanup()` 해야 요소 중복이 안 생긴다.

## 백로그 (별개, 보류 중)
- **M6 윈도우 트랙 완료**: PR #2·#3·#4 머지. 잔여(선택): codex·claude-code `.local\bin` PATH 윈도우 실기 확인.
- **카탈로그 "실제 설치 감지" 보류**(2026-07-08 Agent Teams 검토): 삭제 안전장치(Dashboard.tsx 경고가 catalog.installed 의존) 훼손·이름충돌 오탐(hermes)·타임아웃 전무 리스크. 재착수 시 "스캔 버튼식+하드 타임아웃(fail-open)+installed.json 불변+stdout 검증".
- 약관은 "명백한 리스크"만 보완한 초안. 유료화·규모 확대 시 변호사 정식 검토 권고(사용자 인지함).

## 인프라 (재사용)
- 레포는 **`needsbuilder/easy-harness`** 하나(소스+릴리스+레시피 번들, public, Apache-2.0). 옛 `needslab-ai/easy-harness-releases`·`-recipes`는 아카이브 상태로 보존(삭제 금지).
- GitHub 개인 계정 `needsbuilder`(Yongbeom Gwon), 조직 `needslab-ai`. 커밋 이메일은 전역 지메일.
- Apple: Team RB6FTGW2DK(Individual), 로컬 키 ~/.tauri/. GitHub 시크릿 9종 등록.
- 랜딩: Vercel 프로젝트 `needslab/easyharness`, 정식 주소 easyharness.needslab.ai, DNS는 Vercel(ns1/ns2.vercel-dns.com).
